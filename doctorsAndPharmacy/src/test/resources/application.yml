spring:
  main:
    banner-mode: off
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  jackson:
    serialization:
      fail-on-empty-beans: false

  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: loxy1517

  liquibase:
    enabled: false

server:
  port: 8080